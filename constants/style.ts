import { scale } from "@/styles/scale";

export const BUTTON_ICON_SIZE = scale.ms(24);
export const BUTTON_ICON_HEIGHT = scale.ms(40);
export const BUTTON_ICON_HIT_SLOP = { top: 10, bottom: 10, left: 10, right: 10 };